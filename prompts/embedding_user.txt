Create a list of three random source prompts, of different length, and each source prompt will have a "SYSTEM:" (Defines how a LLM should act.), "USER:" (A random input a user might use for the "SYSTEM:" prompt), and "ASSISTANT:" (This will hold the correct response our LLM should generate, to use for testing.) . Do not include any explanations, only provide a RFC8259 compliant JSON response following this format without deviation.